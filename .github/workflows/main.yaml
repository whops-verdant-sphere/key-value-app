name: "Main Workflow"

#trigger
on:
  push:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    name: "build"
    uses: ./.github/workflows/build.yaml
    with:
      version: "8.2"

  test:
    name: "Test"
    needs: [build]
    uses: ./.github/workflows/test.yaml