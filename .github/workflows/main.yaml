name: "Main Workflow"

#trigger
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    name: build
    uses: ./.github/workflows/build.yaml

  test:
    name: Test
    needs: [build]
    uses: ./.github/workflows/test.yaml