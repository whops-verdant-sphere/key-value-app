name: "Main Workflow"

#trigger
on:
  push:
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    name: build
    uses: ./.github/workflows/build.yaml
    with:
      version: "8.3"

  test:
    name: Test
    needs: [build]
    uses: ./.github/workflows/test.yaml